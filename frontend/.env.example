# Local development (default backend)
# VITE_API_URL=http://127.0.0.1:8000

# Production (optional): set in Vercel → Settings → Environment Variables
# Plans & PDFs work without the API (bundled in /public/project-files).
# Set this when the Render backend is live — enables enquiries + live progress.
# VITE_API_URL=https://malkar-api.onrender.com
